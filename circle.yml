## Customize checkout
checkout:
  post:

## Customize dependencies
dependencies:
  pre:
    - sudo apt-get update
  override:
    - sudo apt-get install libcppunit-dev
    - sudo apt-get install valgrind

test:
  override:
    - ./bootstrap
    - make
    - make check
